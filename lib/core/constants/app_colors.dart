import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // // Primary
  // static const Color primary = Color(0xFF7C3AED);
  // static const Color primaryLight = Color(0xFFA78BFA);
  // static const Color primaryDark = Color(0xFF5B21B6);
  //
  // // ── Light Backgrounds ──────────────────────────────────
  // static const Color background = Color(0xFFF8F7FC);
  // static const Color heroBackground = Color(0xFFF3F0FF);
  // static const Color cardBackground = Color(0xFFFFFFFF);
  // static const Color profileCardBg = Color(0xFFF5E6DA);
  //
  // // ── Light Text ─────────────────────────────────────────
  // static const Color textPrimary = Color(0xFF1A1A2E);
  // static const Color textSecondary = Color(0xFF6B7280);
  // static const Color textWhite = Color(0xFFFFFFFF);
  //
  // // ── Light Borders ──────────────────────────────────────
  // static const Color border = Color(0xFFE5E7EB);
  // static const Color borderLight = Color(0xFFF3F4F6);
  //
  // // ── Light Badge ────────────────────────────────────────
  // static const Color badgeBg = Color(0xFFF3F0FF);
  // static const Color badgeText = Color(0xFF7C3AED);
  //
  // // ── Light Navbar ───────────────────────────────────────
  // static const Color navbarBg = Color(0xFFFFFFFF);
  // static const Color navLinkText = Color(0xFF4B5563);
  // static const Color navLinkHover = Color(0xFF7C3AED);
  //
  // // ── Dark Backgrounds ───────────────────────────────────
  // static const Color darkBackground = Color(0xFF0F0F23);
  // static const Color darkSurface = Color(0xFF16162A);
  // static const Color darkCard = Color(0xFF1E1E36);
  // static const Color darkHero = Color(0xFF1A153A);
  // static const Color darkNavbar = Color(0xFF13132A);
  // static const Color darkBorder = Color(0xFF2E2E50);
  // static const Color darkBorderLight = Color(0xFF252545);
  //
  // // ── Dark Text ──────────────────────────────────────────
  // static const Color darkText = Color(0xFFE5E7EB);
  // static const Color darkTextSecondary = Color(0xFF9CA3AF);
  //
  // // ── Dark Badge ─────────────────────────────────────────
  // static const Color darkBadgeBg = Color(0xFF2D1B69);
  // static const Color darkBadgeText = Color(0xFFA78BFA);

// // ── Primary ─────────────────────────────────────────────
//   static const Color primary = Color(0xFF2563EB);
//   static const Color primaryLight = Color(0xFF60A5FA);
//   static const Color primaryDark = Color(0xFF1E40AF);
//
// // ── Light Backgrounds ──────────────────────────────────
//   static const Color background = Color(0xFFF8FAFC);
//   static const Color heroBackground = Color(0xFFEFF6FF);
//   static const Color cardBackground = Color(0xFFFFFFFF);
//   static const Color profileCardBg = Color(0xFFE0F2FE);
//
// // ── Light Text ─────────────────────────────────────────
//   static const Color textPrimary = Color(0xFF0F172A);
//   static const Color textSecondary = Color(0xFF64748B);
//   static const Color textWhite = Color(0xFFFFFFFF);
//
// // ── Light Borders ──────────────────────────────────────
//   static const Color border = Color(0xFFE2E8F0);
//   static const Color borderLight = Color(0xFFF1F5F9);
//
// // ── Light Badge ────────────────────────────────────────
//   static const Color badgeBg = Color(0xFFE0F2FE);
//   static const Color badgeText = Color(0xFF0369A1);
//
// // ── Light Navbar ───────────────────────────────────────
//   static const Color navbarBg = Color(0xFFFFFFFF);
//   static const Color navLinkText = Color(0xFF475569);
//   static const Color navLinkHover = Color(0xFF2563EB);
//
// // ── Dark Backgrounds ───────────────────────────────────
//   static const Color darkBackground = Color(0xFF020617);
//   static const Color darkSurface = Color(0xFF0F172A);
//   static const Color darkCard = Color(0xFF1E293B);
//   static const Color darkHero = Color(0xFF172554);
//   static const Color darkNavbar = Color(0xFF020617);
//   static const Color darkBorder = Color(0xFF334155);
//   static const Color darkBorderLight = Color(0xFF1E293B);
//
// // ── Dark Text ──────────────────────────────────────────
//   static const Color darkText = Color(0xFFE2E8F0);
//   static const Color darkTextSecondary = Color(0xFF94A3B8);
//
// // ── Dark Badge ─────────────────────────────────────────
//   static const Color darkBadgeBg = Color(0xFF1E3A8A);
//   static const Color darkBadgeText = Color(0xFF93C5FD);

// // ── Primary ─────────────────────────────────────────────
//   static const Color primary = Color(0xFF0D9488); // Teal
//   static const Color primaryLight = Color(0xFF5EEAD4);
//   static const Color primaryDark = Color(0xFF0F766E);
//
// // ── Light Backgrounds ──────────────────────────────────
//   static const Color background = Color(0xFFF8FAFC);
//   static const Color heroBackground = Color(0xFFF0FDFA);
//   static const Color cardBackground = Color(0xFFFFFFFF);
//   static const Color profileCardBg = Color(0xFFE0F2F1);
//
// // ── Light Text ─────────────────────────────────────────
//   static const Color textPrimary = Color(0xFF0B132B);
//   static const Color textSecondary = Color(0xFF6B7280);
//   static const Color textWhite = Color(0xFFFFFFFF);
//
// // ── Light Borders ──────────────────────────────────────
//   static const Color border = Color(0xFFE5E7EB);
//   static const Color borderLight = Color(0xFFF1F5F9);
//
// // ── Light Badge ────────────────────────────────────────
//   static const Color badgeBg = Color(0xFFCCFBF1);
//   static const Color badgeText = Color(0xFF0F766E);
//
// // ── Light Navbar ───────────────────────────────────────
//   static const Color navbarBg = Color(0xFFFFFFFF);
//   static const Color navLinkText = Color(0xFF475569);
//   static const Color navLinkHover = Color(0xFF0D9488);
//
// // ── Dark Backgrounds ───────────────────────────────────
//   static const Color darkBackground = Color(0xFF020617);
//   static const Color darkSurface = Color(0xFF0B132B);
//   static const Color darkCard = Color(0xFF111827);
//   static const Color darkHero = Color(0xFF042F2E);
//   static const Color darkNavbar = Color(0xFF020617);
//   static const Color darkBorder = Color(0xFF1F2937);
//   static const Color darkBorderLight = Color(0xFF111827);
//
// // ── Dark Text ──────────────────────────────────────────
//   static const Color darkText = Color(0xFFE5E7EB);
//   static const Color darkTextSecondary = Color(0xFF9CA3AF);
//
// // ── Dark Badge ─────────────────────────────────────────
//   static const Color darkBadgeBg = Color(0xFF134E4A);
//   static const Color darkBadgeText = Color(0xFF5EEAD4);

// // ── Primary ─────────────────────────────────────────────
//   static const Color primary = Color(0xFFE11D48);
//   static const Color primaryLight = Color(0xFFFB7185);
//   static const Color primaryDark = Color(0xFF9F1239);
//
// // ── Light Backgrounds ──────────────────────────────────
//   static const Color background = Color(0xFFFFF1F2);
//   static const Color heroBackground = Color(0xFFFFE4E6);
//   static const Color cardBackground = Color(0xFFFFFFFF);
//   static const Color profileCardBg = Color(0xFFFFE4E6);
//
// // ── Light Text ─────────────────────────────────────────
//   static const Color textPrimary = Color(0xFF1F2937);
//   static const Color textSecondary = Color(0xFF6B7280);
//   static const Color textWhite = Color(0xFFFFFFFF);
//
// // ── Light Borders ──────────────────────────────────────
//   static const Color border = Color(0xFFFECACA);
//   static const Color borderLight = Color(0xFFFFF1F2);
//
// // ── Light Badge ────────────────────────────────────────
//   static const Color badgeBg = Color(0xFFFECDD3);
//   static const Color badgeText = Color(0xFF9F1239);
//
// // ── Light Navbar ───────────────────────────────────────
//   static const Color navbarBg = Color(0xFFFFFFFF);
//   static const Color navLinkText = Color(0xFF4B5563);
//   static const Color navLinkHover = Color(0xFFE11D48);
//
// // ── Dark Backgrounds ───────────────────────────────────
//   static const Color darkBackground = Color(0xFF1F0A10);
//   static const Color darkSurface = Color(0xFF2A0F16);
//   static const Color darkCard = Color(0xFF3A0F1C);
//   static const Color darkHero = Color(0xFF4C0519);
//   static const Color darkNavbar = Color(0xFF1F0A10);
//   static const Color darkBorder = Color(0xFF7F1D1D);
//   static const Color darkBorderLight = Color(0xFF3A0F1C);
//
// // ── Dark Text ──────────────────────────────────────────
//   static const Color darkText = Color(0xFFFFE4E6);
//   static const Color darkTextSecondary = Color(0xFFFDA4AF);
//
// // ── Dark Badge ─────────────────────────────────────────
//   static const Color darkBadgeBg = Color(0xFF881337);
//   static const Color darkBadgeText = Color(0xFFFDA4AF);

// ── Primary ─────────────────────────────────────────────
  static const Color primary = Color(0xFFF59E0B); // Amber
  static const Color primaryLight = Color(0xFFFCD34D);
  static const Color primaryDark = Color(0xFFB45309);

// ── Light Backgrounds ──────────────────────────────────
  static const Color background = Color(0xFFFAFAFA);
  static const Color heroBackground = Color(0xFFFFFBEB);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color profileCardBg = Color(0xFFFEF3C7);

// ── Light Text ─────────────────────────────────────────
  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF525252);
  static const Color textWhite = Color(0xFFFFFFFF);

// ── Light Borders ──────────────────────────────────────
  static const Color border = Color(0xFFE5E5E5);
  static const Color borderLight = Color(0xFFF5F5F5);

// ── Light Badge ────────────────────────────────────────
  static const Color badgeBg = Color(0xFFFEF3C7);
  static const Color badgeText = Color(0xFF92400E);

// ── Light Navbar ───────────────────────────────────────
  static const Color navbarBg = Color(0xFFFFFFFF);
  static const Color navLinkText = Color(0xFF404040);
  static const Color navLinkHover = Color(0xFFF59E0B);

// ── Dark Backgrounds ───────────────────────────────────
  static const Color darkBackground = Color(0xFF0A0A0A);
  static const Color darkSurface = Color(0xFF171717);
  static const Color darkCard = Color(0xFF262626);
  static const Color darkHero = Color(0xFF451A03);
  static const Color darkNavbar = Color(0xFF0A0A0A);
  static const Color darkBorder = Color(0xFF404040);
  static const Color darkBorderLight = Color(0xFF262626);

// ── Dark Text ──────────────────────────────────────────
  static const Color darkText = Color(0xFFE5E5E5);
  static const Color darkTextSecondary = Color(0xFFA3A3A3);

// ── Dark Badge ─────────────────────────────────────────
  static const Color darkBadgeBg = Color(0xFF78350F);
  static const Color darkBadgeText = Color(0xFFFCD34D);
}
